# 작업 내역 — GitHub 레포 연결

**일시:** 2026-05-22  
**대상 레포:** https://github.com/LeeJae-Yeong/KBO_PROJECT.git

## 수행 내용

1. 로컬 폴더 `C:\kbo project`에 Git 저장소 초기화 (`git init`)
2. 원격 저장소 `origin` 등록
   - URL: `https://github.com/LeeJae-Yeong/KBO_PROJECT.git`
3. 기본 브랜치를 `main`으로 설정 (`git branch -M main`)

## 변경된 파일/경로

- `.git/` — 새로 생성 (저장소 메타데이터)
- `.git/config` — `origin` 원격 URL 설정

## 현재 상태

- 로컬: `main` 브랜치, `origin/main` 추적 설정 완료
- GitHub: `main` 브랜치에 초기 커밋 푸시 완료 (커밋 `4047b33`)

## 2026-05-22 추가 작업 — 첫 푸시

- 커밋 메시지: `초기 커밋: GitHub 원격 연결 및 작업 내역 추가`
- `git push -u origin main` 성공

## 이후 첫 푸시 방법

프로젝트 파일을 추가한 뒤:

```powershell
cd "C:\kbo project"
& "C:\Program Files\Git\cmd\git.exe" add .
& "C:\Program Files\Git\cmd\git.exe" commit -m "초기 커밋"
& "C:\Program Files\Git\cmd\git.exe" push -u origin main
```

> PowerShell에서 `git` 명령이 인식되지 않으면 위처럼 Git 설치 경로의 전체 경로를 사용하거나, 시스템 PATH에 `C:\Program Files\Git\cmd`를 추가하세요.
